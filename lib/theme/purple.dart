import 'package:fitness/theme/themes/commons/app_theme.dart';

import 'colors.dart';

const _primaryColor = AppColors.primaryColor;

final purpleLight = createAppLightTheme(
  primaryColor: _primaryColor,
);

final purpleDark = createAppDarkTheme(
  primaryColor: _primaryColor,
);